import styled from 'styled-components';

export const ProductName = styled.h3`
  display: -webkit-box;
  -webkit-line-clamp: 2;
  font-weight: normal;
  -webkit-box-orient: vertical;
  overflow: hidden;
`;